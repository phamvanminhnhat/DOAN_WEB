@model IEnumerable<VPP18.Models.DANHMUCSANPHAM>

@{
    ViewBag.Title = "Danhmuc";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>

    
        
            <table class="table">
                <tr>
                    <th>
                        Mã danh mục
                    </th>
                    <th>
                        Tên danh mục
                    </th>
                    <th>
                        Hình ảnh
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.IdDM)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TenDM)
                        </td>
                        <td>
                            <img style="width:150px" src="@item.HinhAnh"/>
                        </td>
                        <td>
                           
                            
                            <a class="btn btn-sm btn-warning" href="@Url.Action("Edit", "Danhmuc", new { id = item.IdDM})"><i class="fas fa-edit"></i> Edit</a>
                            @Html.ActionLink("Details", "Detail", new { id = item.IdDM }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.IdDM }) |

                        </td>
                    </tr>
                }

            </table>

       
        

